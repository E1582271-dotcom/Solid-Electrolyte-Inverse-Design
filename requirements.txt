# Project 3 -- screening + scoring + figure environment (Python 3.10-3.12).
# Stage A (MatterGen generation) is a SEPARATE Python 3.10 environment; install it via
# MatterGen's own instructions (see HPC_VANDA.md / notebooks/01_mattergen_pipeline.ipynb),
# NOT from this file.

numpy
pandas
matplotlib
pymatgen
ase
mace-torch          # MACE-MP-0 universal MLIP for the stability screen (GPU recommended)
catboost            # runs the vendored Project-1 conductivity model (vendor/)

# optional -- only to REGENERATE data/ref_structures.json from Materials Project.
# A portable copy is already shipped, so offline runs do not need this.
mp-api
