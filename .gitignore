# secrets
mp_api_key.txt
*.key

# runtime products regenerated by the pipeline (01–04). The LJ plumbing fixtures and the
# intermediate per-run outputs are NOT shipped; the final portfolio deliverables ARE — the
# candidates table, the run manifest, the reference-structure cache, and the two publication
# figures — so the repo tells its whole story on a fresh clone (mirrors Project 1/2's tracked
# metrics/figures). Those tracked deliverables are simply omitted from the ignore list below.
data/generated/
data/relaxed/
data/mattergen_run/
data/ref_entries.pkl
data/*_cache*.json
data/screened.csv
data/scored.csv
figures/*.png
figures/*.tiff
!figures/fig_generation_funnel.png
!figures/fig_inverse_design.png

# MLIP / generative model-weight caches downloaded at runtime
*.model
*.pth
*.ckpt
checkpoints/

# python / notebooks / os
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/
.DS_Store
Thumbs.db
